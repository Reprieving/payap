server:
  port: 2000

spring:
  application:
    name: account
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: configuration
      fail-fast: true




